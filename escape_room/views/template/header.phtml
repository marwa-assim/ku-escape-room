<!--
 * Header Template
 *
 * This template contains the opening HTML structure, meta tags, navigation bar,
 * and opening of the main content area. It should be included at the top of
 * every page.
 *
 * Author: Eshraq Ebrahim
 * Version: 1.0
 * Since: 2026-9-17
 * Last Modified: 2026-9-17
-->

<?php
if (session_status() === PHP_SESSION_NONE)
{
    session_start();
}

$isLoggedIn = !empty($_SESSION['user_id']);
$isAdmin = $isLoggedIn && !empty($_SESSION['is_admin']);
$username = $isLoggedIn ? (isset($_SESSION['username']) ? $_SESSION['username'] : 'User') : null;
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo isset($pageTitle) ? $pageTitle : 'Escape Room'; ?></title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/system-style.css">

    <?php if (!empty($extraCss)): ?>
        <?php foreach ((array) $extraCss as $cssFile): ?>
            <link rel="stylesheet" href="<?php echo htmlspecialchars($cssFile); ?>">
        <?php endforeach; ?>
    <?php endif; ?>
</head>
<body>

<?php if ($showNav ?? true): ?>
    <nav class="navbar navbar-expand navbar-dark bg-dark px-3">
        <a class="navbar-brand" href="index.php">Escape Room</a>
        <div class="navbar-nav mx-auto">
            <a class="nav-link" href="index.php">Home</a>
            <a class="nav-link" href="player.php?setup=1">Player Screen</a>
            <a class="nav-link" href="puzzle.php?setup=1">Puzzle Setup</a>
            <?php if ($isLoggedIn): ?>
                <a class="nav-link" href="support.php">Support Panel</a>
                <a class="nav-link" href="login.php?action=logout">Logout (<?php echo htmlspecialchars($username); ?>)</a>
            <?php else: ?>
            <a class="nav-link" href="login.php">Support Login</a>
            <?php endif; ?>
        </div>
    </nav>
<?php endif; ?>

<!-- Main content -->
<main class="container mt-4">
